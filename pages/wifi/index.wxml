<!--index.wxml-->
<view>
  <view  wx:for="{{wifiList}}" class="item" wx:key="item.id">
      <view class="title">{{item.name}}</view>
      <view class="intro">{{item.intro}}</view>
  </view>
</view>
